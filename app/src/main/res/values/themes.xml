<resources>
    <!-- Base application theme. -->
    <style name="Theme.stadtiq" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <!-- Add secondary colors if needed -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <!-- Theme for activities that use the Toolbar added in layout (disables default ActionBar) -->
    <style name="Theme.stadtiq.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Overlay themes for Toolbar popups if needed -->
    <style name="ThemeOverlay.AppCompat.Dark.ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- Colors specific to the dark overlay -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <!-- Add other colors as needed -->
    </style>
    <style name="ThemeOverlay.AppCompat.Light" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- Colors specific to the light overlay (e.g., for popup menus) -->
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>


    <!-- Style for the horizontally scrollable value/time options -->
    <style name="ValueOptionStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minWidth">80dp</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">8dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:background">@drawable/value_option_selector</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Small</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

</resources>